# Copy to ".env" and set your key.
# Personal OpenAI API key (platform.openai.com -> API keys). Pay-as-you-go.
OPENAI_API_KEY=sk-...

# Model used by the agents. A small model keeps a scan cheap (many calls).
GEO_MODEL=gpt-4o-mini
